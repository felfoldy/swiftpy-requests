.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/
.netrc

# Xcode per-user scheme ordering
xcschememanagement.plist

# SPM resolved versions — not meaningful for a library; consumers resolve their own.
/Package.resolved
